<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Route</title>
</head>
<body>
<a href="{% url 'pos:trip_detail' pk=route.trip.id%}">Back</a>
<h1>Edit Route</h1>
<form action="{% url 'pos:route_edit' pk=route.id %}" method="post">
    {% csrf_token %}
    <p>Route Index: {{ route.index }}</p>
    {{ form.as_p }}

    {{ orderitems.management_form }}
    {% for oi_form in orderitems %}
        {% if forloop.first %}
            <p>Customer Name: {{ oi_form.instance.customerproduct.customer.name }}</p>
        {% endif %}
        <p>Product Name: {{ oi_form.instance.customerproduct.product.name }}</p>
        {{ oi_form.as_p }}
    {% endfor %}

    <input type="submit" value="Save">
</form>

</body>
</html>